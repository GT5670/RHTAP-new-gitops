. In the *Image Registry* field, specify the on-premises image registry URL without the `HTTP` protocol. The image registry can be quay or jfrog artifactory. For example,  quay.io or  `tssc.jfrog.io`
